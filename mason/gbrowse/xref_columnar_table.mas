<%args>
  $xrefs => []
</%args>
  <& /page/columnar_table.mas,
            headings=>["Annotation Dataset", "Location(s)"],
            data=> [ map [ $_->data_source->description,
                           '<a href="'.$_->url.'">'
                           .join( ', ', (
                               map $_->seq_id.':'.$_->start.'..'.$_->end, @{$_->seqfeatures}
                                  )
                             )
                           .'</a>',
                          ],
                     @$xrefs
                   ],
            __align=> 'l'
  &>
