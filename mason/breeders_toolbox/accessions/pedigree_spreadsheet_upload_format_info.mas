<%args>
</%args>

<div id="pedigrees_upload_spreadsheet_info_dialog" >
<b>
Pedigrees may be uploaded in tab-delimited text file format
</b>
<br>
(.xls or .xlsx format not supported)
<br><br>
<b>
  Header:
</b>
<br>
The first row (header) should contain the following:
<br>

<table cellspacing="1" cellpadding="2" border="1">
  <tr>
    <td>progeny name</td>
    <td>female parent accession</td>
    <td>male parent accession</td>
  </tr>
</table>

<br>

<h4>Required fields:</h4>
<ul>
<li>progreny name (must exist in the database)</li>
<li>female parent accession (must exist in the database)</li>
</ul>

<h4>Optional fields</h4>
<ul>
<li>male parent accession (if provided, must exist in the database)</li>
</ul>

<h4>Notes</h4>
<ul>
<li>If female parent equals male parent, a "self" is assumed.</li>
<li>If male is ommitted, "open pollination" is assumed.</li>
</ul>

</div>
